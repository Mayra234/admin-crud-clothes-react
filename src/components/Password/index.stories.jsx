import React from 'react';
import { Password } from '../Password';

export default { title: 'Components/Password' };

export const Overview = () => {
  return <Password name="password" label="Contraseña" />;
};
